<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PlaceUr" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryDarkColor</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryLightColor</item>
        <item name="colorSecondaryVariant">@color/secondaryDarkColor</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/statusBarColor</item>

        <!--  Background Color      -->
        <item name="android:windowBackground">@color/colorBackground</item>
        <!-- Customize your theme here. -->

        <item name="colorSurface">@color/white</item>

    </style>


    <!-- The theme without ActionBar -->
    <style name="Theme.PlaceUr.NoActionBar" parent="Theme.PlaceUr">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowContentTransitions">true</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents">
        <item name="android:fontFamily">@font/google_sans</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents">
        <item name="android:fontFamily">@font/google_sans</item>
    </style>

    <style name="BottomNavigation" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:background">@color/colorCardBackground</item>
<!--        <item name="itemBackground">@drawable/colo</item>-->
        <item name="itemIconTint">@color/colorNumber</item>
        <item name="itemTextColor">@color/colorNumber</item>
        <item name="paddingStart">@dimen/dimen_4</item>
        <item name="paddingEnd">@dimen/dimen_4</item>
    </style>

</resources>