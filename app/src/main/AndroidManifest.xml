<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.imutable.coding.placeur">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".PlaceURApplication"
        android:fullBackupOnly="true"
        android:icon="@drawable/icon_login"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.PlaceUr">
        <activity android:name=".view.place.HomeActivity"
            android:launchMode="singleTask">

        </activity>
        <activity
            android:name=".view.user.SignUpActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.PlaceUr.NoActionBar" />
        <activity
            android:name=".view.user.LoginActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.PlaceUr.NoActionBar" />
        <activity
            android:name=".view.user.SplashActivity"
            android:theme="@style/Theme.PlaceUr.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>